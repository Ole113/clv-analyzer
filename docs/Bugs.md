# Reported Bugs

# New

- The PP dabble screen still has a very wide checkbox that is NOT fixed. the checkbox hides the slip + button.
- - The PP checkbox is shown over the side panel window when you expand it. Change the z-index to be below it
- The default grouping should be by the most recently added bet on the /bets page
- All columns should be sortable on the /bets page.
- Your filter and sort settings should be saved when you navigate from /bets to a specific bet and then press the "Back" button
- The "group by day" button should be on the same line as the initial "25 picks · 20 awaiting close · 0 settled · 6 graded" text
- The "Settings" text on the sidebar in the settings gets hidden when you scroll down the settings page.
- If the grade fails include a link to the ESPN page that the grade couldn't be found on
- In /bets the "Books" dropdown needs to have the book names formatted so they're not hyphen separated and both words are capitalized. For example, "Dogg-house" becomes "Dogg House"
- The when taken dropdown option should say "Live" not "In-play"
- The "Live" tag should be next to the player name on /bets not below it
- no PropProfessor market alias for "Longest Reception". Add it to MARKET_ALIASES.

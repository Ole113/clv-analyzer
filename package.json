{
  "name": "clv-analyzer",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["shared", "server", "extension"],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace extension",
    "test": "npm run test --workspace server"
  }
}
